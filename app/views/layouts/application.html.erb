<!DOCTYPE html>
<html>
<head>
  <title>Inv Manager</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>

<body>

  <div class="container" id="main">

    <div class="navbar navbar-fixed-top navbar-default">
      <div class="container">
        
        <a class="navbar-brand" href="/"><%= image_tag("logo.png", alt: "My Logo") %></a>
        <ul class="nav navbar-nav">
          <li class="active"><%= link_to "Home", root_path %></li>
          <li><%= link_to "Spage2", spage2_path %></li>
        </ul><!-- end nav left -->
                   
        <ul class="nav navbar-nav pull-right">
        <% if logged_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><span class="glyphicon glyphicon-user"></span> My Account <b class="caret"></b></a>
            <ul class="dropdown-menu">
								<li><a><small> Logged in as</small><br><strong><%= current_user.name %></strong></a></li>
								<li class="divider"></li>
								<li><%= link_to '<i class="glyphicon glyphicon-wrench"></i> Profile'.html_safe, current_user %></li>									
								<li><%= link_to '<i class="glyphicon glyphicon-refresh"></i> Settings'.html_safe, edit_user_path(current_user) %></li>									
								<li><a href="#"><span class="glyphicon glyphicon-briefcase"></span> Billing</a></li>									
								<li class="divider"></li>									
								<li><%= link_to '<i class="glyphicon glyphicon-off"></i> Logout'.html_safe, logout_path, method: "delete" %></li>
						</ul>
          </li>  
        <% else %>
          <li class="active"><%= link_to '<i class="glyphicon glyphicon-user"></i> Login'.html_safe, login_path %></li>
        <% end %>
        </ul><!-- end nav right -->
                   
      </div><!-- end navbar container -->
    </div><!-- end navbar -->

    <% flash.each do |message_type, message| %>
      <div class="alert alert-<%= message_type %>" id="greenAlert"><%= message %></div>
    <% end %>
    
<%= yield %>
<%= debug(params) if Rails.env.development? %>      
  </div><!-- end main container -->

</body>
</html>
